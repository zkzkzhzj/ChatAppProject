# 로컬 개발 환경 프로파일.
# IDE에서 실행 시 -Dspring.profiles.active=local 로 활성화한다.

jwt:
  secret: local-dev-only-secret-key-must-be-at-least-256-bits-long!!

security:
  env-public-paths:
    - /actuator/**    # 로컬에서는 actuator 전체 허용
