# 프로필을 타지 않는 공통 설정.
# 환경별 설정은 application-{profile}.yml에서 관리한다.

spring:
  application:
    name: gohyang

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  security:
    filter:
      order: 10

server:
  port: 8080
