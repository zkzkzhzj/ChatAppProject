# 2026-04-05

> 상세 기술 기록은 `/docs/learning/`에, 현재 상태 스냅샷은 `/docs/handover.md`에 있다.

---

## ERD 물리 설계 확정

**결정 사항:**

- CHARACTER ↔ USER: **1:1** 확정 (`CHARACTER.user_id UNIQUE` 제약)
- SPACE ↔ USER: **1:N** 유지 (`is_default`로 기본 공간 구분)
- 아이템 장착/배치: `USER_ITEM_INVENTORY` 직접 참조 없이 `ITEM_DEFINITION`만 참조
  - 소유 검증은 애플리케이션 레이어에서 수행
  - 아이템은 소모재가 아닌 소유재 — 장착/배치해도 인벤토리에서 사라지지 않음
  - `SPACE_PLACEMENT`는 동일 아이템 무제한 배치 허용 (수량 컬럼 없음)
- `POINT_WALLET` 분리 유지: transaction 합산 방식 대신 스냅샷 + 낙관적 락 방식 채택
  - 이유: 동시 차감 시 레이스 컨디션 방지, O(1) 잔액 조회

**추가된 파일:**

- `docs/architecture/erd.mermaid` — 전체 물리 ERD Mermaid 다이어그램 (로컬 전용, .gitignore)
- `docs/history/2026-04-05.md` — 이 파일

**변경된 파일:**

- `docs/architecture/erd.md` — CHARACTER UNIQUE 추가, 카디널리티 요약 섹션, 설계 결정 기록 추가
- `.gitignore` — `*.mermaid` 추가
- `docs/handover.md` — ERD 완료 반영, 다음 작업 후보 업데이트

---

## Happy Path 및 구현 로드맵 확정

**결정 사항:**

- Happy Path: "신규 유저가 마을에서 AI NPC와 첫 대화를 나눈다"
  - 소셜 로그인은 Happy Path 이후. 먼저 이메일 회원가입으로 속도 확보.
  - NPC 응답은 하드코딩으로 시작. 로컬 LLM/Claude API는 별도 Phase.
- 구현 순서: Phase 0(Flyway) → 1(Identity) → 2(Village) → 3(Communication) → 4(Economy) → 5(AI NPC)
- 서브에이전트/스킬 세팅은 Happy Path 완료 후 반복 패턴 파악되면 도입
  - 이유: 뭘 자동화할지 모르는 상태에서 도구부터 만들면 낭비

**추가된 파일:**

- `docs/planning/phases.md` — Phase별 구현 로드맵

**변경된 파일:**

- `docs/handover.md` — 현재 Phase 명시 (Phase 0 진행 전)
- `CLAUDE.md` — Document Routing에 phases.md 추가
