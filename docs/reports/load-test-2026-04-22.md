# 부하 테스트 리포트 — 2026-04-22

> 마을 공개 채팅 + 위치 공유 혼합 부하의 breaking point 좌표 확정을 위한 k6 실측 기록.
> 관련 학습노트: [41. k6 사용법·설계](../learning/41-k6-load-testing-setup.md) · [40. 관측성 스택](../learning/40-observability-stack-decisions.md) · [42. Grafana JVM 4701 리딩](../learning/42-grafana-jvm-dashboard-reading.md)

---

## 1. 테스트 환경

| 항목 | 값 |
|------|-----|
| 백엔드 | `gohyang-server-ko` EC2 (서울) · Spring Boot 4.0.3 / Java 21 |
| JVM Heap Max | 256 MiB (컨테이너 `-Xmx256m`) |
| 경로 | Cloudflare 경유 (`wss://ghworld.co/ws/websocket`) |
| NPC 어댑터 | `NPC_ADAPTER=hardcoded` (OpenAI 호출 제외, 순수 시스템 한계 측정 목적) |
| 부하 도구 | k6 v1.7.1 · raw WebSocket 위 STOMP 프레임 수동 조립 |
| 토큰 풀 | `loadtest/tokens.json` · 1000 유저 JWT (`loadtest-0001~1000@test.local`) |
| 관측 스택 | monitor EC2 (`gohyang-monitor`) · Prometheus + Grafana · Spring Boot Actuator `/actuator/prometheus` scrape 15s |

### 시나리오 파일

- `loadtest/village-mixed.js` — 1 VU 당 position 500ms(±jitter) + chat 15~30s 랜덤. SUBSCRIBE `/topic/chat/village` + `/topic/village/positions`.

---

## 2. 실행 이력

### 2.1 C-3 스모크 — VU 1 / 30s (2026-04-22 15:26)

**목적**: 시나리오 스크립트 동작·CF 통과·STOMP 인증 검증.

| 지표 | 값 |
|------|-----|
| handshake | 101 (성공) ✓ |
| `stomp_connect_latency` p95 | 130 ms |
| `stomp_errors` | 0 |
| `position_sent` / `received` | 55 / 61 |
| `chat_sent` / `received` | 1 / 1 |
| `ws_session_duration` | 30.35 s |
| data rx / tx | 22 KB / 11 KB |

**관찰**:

- CF가 k6 User-Agent(`k6/1.7.1`) 차단 안 함. 403/captcha 없음
- raw WS `/ws/websocket`이 SockJS 엔드포인트 뒤에서 정상 동작
- `position_received(61) > position_sent(55)` 차이 6 — 동시간대 다른 세션이 마을에 있었음. Simple Broker가 sender 포함 전원에게 fan-out 한다는 것도 같이 검증됨

**트랩·해결**:

- k6 v1.7 런타임에 `URL` 글로벌 없음 → regex 파싱으로 대체
- `socket` host 객체에 임의 속성 할당 불가 → 클로저 변수로 전환

---

### 2.2 C-3.5 중간 스모크 — VU 10 / 60s (2026-04-22)

**목적**: Grafana 패널 움직임 + 10배 fan-out 실측 검증.

| 지표 | 값 | 해석 |
|------|-----|------|
| checks | 20/20 ✓ | 10 VU × 2 iteration 전부 성공 |
| `stomp_connect_latency` p95 | 225 ms | 여유 (threshold 3000ms) |
| `stomp_errors` | 0 | — |
| `position_sent` | 1,165 | 10 VU × ~58 msgs/30s |
| `position_received` | **11,680** | sent의 **10.02배** |
| `chat_sent` / `received` | 20 / 200 | sent의 **10배** |
| `ws_sessions` | 20 | VU당 30s 세션 × 2회 |
| data rx / tx | 3.4 MB / 219 KB | — |

**관찰**:

- **Fan-out 비율이 VU 수와 정확히 일치** — Simple Broker 동작 검증됨
- 서버 입장에선 여전히 거의 idle. 병목 징후 0
- position이 네트워크 트래픽을 지배 (chat 20건 vs position 1165건)

---

### 2.3 C-4 정식 부하 — 1차 실패 🔴 (2026-04-22 17:00 KST)

**커맨드 실행**: `--stage 1m:50 --stage 3m:500 --stage 5m:1000 --stage 2m:0`

**결과**: 테스트 시작 즉시 전량 실패. 6분 누적 시점 기준 지표.

| 지표 | 값 |
|------|-----|
| `handshake failed: status=0` | **108,231건** |
| `close 1002 (protocol error)` | **42,847건** |
| `STOMP ERROR` (빈 body) | **42,834건** |
| `CONNECTED` 프레임 수신 | **0건** |
| 성공 iteration | 없음 |

**유저의 Grafana 실시간 관찰**:

- Heap Total 24 ↔ 53% 오가는 sawtooth
- HTTP Request Rate `/ws/**` 보라색 급등 (VU들이 반복 재시도)
- CPU Process 40%
- G1 Old Gen 49.7 → 128 MiB (수만 건 예외+로그 객체 promotion)
- HTTP p95/p99 No Data 유지 (reject가 너무 빨라 bucket 바닥)
- Threads Live 58 → 250 (Tomcat WS upgrade 처리용 thread pool 확장)
- **ghworld.co Phaser 화면에 접속자 0명** (k6가 SUBSCRIBE까지 못 감 → position broadcast 없음)

**Kill switch 발동**: 3조건 모두 크게 초과 → 수동 중단.

### 2.4 근본 원인 분석 — JWT 만료 × 토큰 풀 재사용

**타임라인**:

| 시각 | 사건 |
|------|------|
| 15:22 KST | `prepare-tokens.js COUNT=1000` — 1000개 JWT 발급 후 `tokens.json` 저장 |
| 17:00 KST | C-4 시작 |
| 경과 | **1시간 38분** |

**백엔드 설정** (`backend/src/main/resources/application.yml:57`):

```yaml
jwt:
  access-token-expiry-ms: 3600000  # 1시간
```

→ 1000개 토큰 **전부 만료 상태**로 테스트 실행됨.

**실패 메커니즘**:

1. k6 → CONNECT 프레임의 `Authorization: Bearer {expired-jwt}` 전송
2. Spring `StompAuthChannelInterceptor` → `parseTokenPort.parse(token)` → `Optional.empty()`
3. `throw new MessageDeliveryException("Invalid or expired token")`
4. Spring STOMP ERROR 프레임 + WS close 1002 (protocol error) 응답
5. k6 iteration 즉시 종료 → 다음 iteration → 무한 반복

**즉, 부하 테스트가 아니라 "만료 JWT rejection 테스트"가 됨**. Cloudflare·백엔드 자체 문제 아님.

**진단 경로** (블로그 Task 1 서사 재료):

```text
handshake 101 OK 1건 확인 → WS 레이어는 정상
→ CONNECTED 프레임 0건 → STOMP 레이어에서 reject
→ close 1002 protocol error → 서버가 명시적으로 STOMP 프레임을 거부
→ StompAuthChannelInterceptor 코드 분석
→ jwt.access-token-expiry-ms = 3600000 (1h) 발견
→ tokens.json 발급 시각 추적 → 1h 38m 경과 확정
```

### 2.6 C-4 2차 실행 — 실제 breaking point 확정 🎯 (2026-04-22 17:21 KST)

**선행**: 토큰 재발급 완료 (login 1000 / 0 errors / 82.5s). 모든 JWT 발급 시각 17:22 KST — 테스트 전 구간에서 만료 여유 확보.

**커맨드**: C-4 1차와 동일 (`--stage 1m:50 --stage 3m:500 --stage 5m:1000 --stage 2m:0`).

**실행 결과**: 약 6분 7초 경과 시점(**VU 713 도달**) 수동 중단.

#### 에러 시작 좌표

| 시각 | 경과 | 이벤트 |
|------|------|--------|
| 17:21:30 (±) | T+0 | 테스트 시작 |
| **17:22:02** | **T+32s** | **VU 53에서 첫 `handshake failed: status=502`** ← **Breaking point** |
| 17:22~17:27 | T+32s ~ T+6m07s | 실패 지속, VU ramp 따라 계속 증가 |
| 17:27:37 (±) | T+6m07s | k6 강제 종료 (kill switch 발동) |

**이전 실패와의 차이**: C-4 1차는 `status=0` (JWT 만료 → WS close 1002) 였으나 이번은 `status=502/520` (HTTP 레이어 자체에서 원본이 5xx 반환). **WS upgrade 단계도 못 감** — 백엔드가 HTTP 요청조차 처리 못 하는 상태.

#### 에러 유형별 카운트 (~6분간 누적)

| 상태 코드 | 건수 | 의미 |
|-----------|------|------|
| `status=0` | 20,712 | CF/중간 네트워크가 연결 drop (응답 없음) |
| `status=520` | 15,310 | CF가 origin에서 invalid 응답 받음 |
| `status=502` | 24 | CF가 origin에서 5xx 응답 받음 |
| **총** | **36,046** | **handshake 완료 건수 0** |

**STOMP ERROR / close 1002 / close 1006 모두 0건** — WS upgrade 자체에 실패하므로 STOMP 레이어까지 도달 못 함.

#### 백엔드 상태 (kill 직후)

| 엔드포인트 | 응답 |
|-----------|------|
| `/actuator/health` | **504 Gateway Timeout** (60s 대기 후) |
| 여러 차례 재시도 | 지속 504 |

k6 중단 후에도 백엔드가 회복 안 됨. Tomcat 쓰레드 풀 전체가 hanging WS 세션에 묶여있거나, 내부 broker dispatch queue가 폭주 상태로 추정.

#### 병목 원인 후보 (유력도 순, Grafana 17:21~17:27 구간 관찰로 확증 필요)

1. **STOMP Simple Broker single-threaded dispatch** ⭐
   - `org.springframework.messaging.simp.broker.SimpleBrokerMessageHandler`가 모든 SUBSCRIBE/SEND를 단일 worker thread로 처리
   - VU당 초당 2회 position SEND → broker가 subscriber 전원(VU_N명)에게 broadcast
   - VU 50 기준: 초당 약 100 inbound × 50명 fan-out = **5,000 dispatch/s**
   - VU 500이면 250,000 dispatch/s — Simple Broker가 감당 못 함
   - 확증 지표: JVM Threads Live 증가 패턴·MessageHandler thread state
2. **Tomcat worker thread pool 고갈 (기본 200)**
   - WS 업그레이드 + 세션 유지 + inbound 처리에 쓰레드 점유
   - 꽉 차면 HTTP 요청(health check 포함)이 queue에서 대기 → CF 504
   - 확증: Threads Live가 200 근처 평탄화 구간 + HTTP Rate 감소
3. **JVM Heap 압박 (256 MiB 상한)**
   - 세션·MessageHeaders·STOMP 프레임 객체 대량 할당
   - G1 Mixed GC 빈번 → 순간 pause가 서비스 응답 차단
   - 확증: Heap 90%+ 진입 · GC Pause Max 급등 · Old Gen 급성장
4. **JWT 파싱·쓰레드 풀 경합**
   - StompAuthChannelInterceptor가 CONNECT마다 ParseTokenPort 호출 → JWT 파싱 CPU 사용
   - 가능성 낮음 (CPU 관찰 필요)

#### 결론 — Breaking Point 좌표

**약 VU 50 내외에서 백엔드 한계 도달**.

- 1000명은 한참 못 미치는 수준
- Phase 3 설계 단계에서 "스케일아웃 시 외부 broker로 교체 예정"이라 문서화한 [docs/learning/15](../learning/15-websocket-stomp-deep-dive.md), [learning/21](../learning/21-village-public-chat-architecture.md)의 가정이 **실측으로 입증**됨
- Simple Broker는 수십 동시 접속 규모까지가 현실적 한계

#### 추가 관찰 — 부하 폭주 시 Prometheus scrape 자체가 실패

사용자가 Grafana 17:21:30~17:27:40 구간 캡처 시도 → "선이 없고 시작 지점에 점 1개만" 관찰.

**원인**: 백엔드가 overload 상태라 `/actuator/prometheus` scrape(15s 주기)도 504 Timeout. Prometheus TSDB에 **공백 구간**이 그대로 남음.

타임라인:

| 시각 | 상태 | Prometheus scrape |
|------|------|-------------------|
| ~17:21:30 | 테스트 시작 | 정상 (1~2 샘플 수집) |
| 17:22 ~ 17:27 | **백엔드 overload** | **504 → 공백** |
| 17:27 ~ 17:30 | k6 kill 후에도 뻗어있음 | 공백 지속 |
| 17:30 | 수동 재기동 | 새 프로세스 메트릭 시작 |
| 17:39 ~ 17:42 | 데모 (VU 20) | 정상 데이터 |

**이 공백 자체가 병목의 증거**. "서버가 너무 뻗어서 모니터링 시스템조차 응답 못 받았다"는 서사는 블로그 Task 1의 시각적 임팩트로도 사용 가능.

**권장 캡처 범위**: `From 17:20:00 To 17:45:00` — 전후 맥락 포함(정상 → 공백 → 재기동 → 정상 데모) 한 장에 담김.

#### 다음 단계

1. 백엔드 회복 — ✅ 수동 재기동 완료 (17:30)
2. Grafana **17:20~17:45** 시각 범위 캡처 — 공백 구간 + 데모 구간 둘 다 포함 (Heap / CPU / Threads / GC Pause / HTTP Rate)
3. 원인 후보 1~4 중 주범 판별
4. **C-5 보강 실험** — 더 세밀한 ramp로 정확한 breaking point 확인:
   - 0 → 10 (30s) → 30 (1m) → 50 (2m) → 80 (2m) → 100 (2m) → 0 (1m)
   - 총 9분, VU 10~100 구간 정밀 스캔

---

### 2.7 병목 주범 확정 — Grafana 5패널 판독 🎯

Grafana 17:12~17:45 KST 시각 범위에서 C-4 2차 실행 시점 (17:20~17:22) 스냅샷 분석.

#### 2.7.1 5개 패널 관찰값 요약

| 지표 | Baseline (17:12~17:19) | Breaking point (17:20~17:22) | 배수 |
|------|----------------------|------------------------------|------|
| **Heap Total %** | 22% | **87%** 🔴 | 3.95x |
| **Threads Live** | 72 | **180+** 🟠 (max-threads 200 턱밑) | 2.5x |
| **CPU Process** | ~0% | **70%** 🟠 (2코어 중 1.4코어) | ∞ |
| **Major GC pause** | 0ms | **1,650ms (1.65초)** 🔴 | ∞ |
| **Minor GC pause** | ~5ms | **440ms** 🟠 | 88x |
| HTTP Rate `/ws/**` | 0 ops/s | 12 ops/s | — |

패널 원본: `loadtest/screenshots/c4-{1,2,3,4,5}-*.png`.

#### 2.7.2 단일 원인 아님 — **3중 동시 포화 + GC Death Spiral**

breaking point는 하나의 병목이 아니라 **연쇄 반응**:

```text
① VU ~50 진입 (17:20 KST)
    ↓
② 50개 WS 세션 · Subscription · MessageHeaders · 각 VU당 초당 2 position SEND
    → Simple Broker가 각 SEND를 50명에게 fan-out
    → 초당 5,000 dispatch = MessageHeaders·Message 객체 대량 생성
    ↓
③ Heap 22% → 87% in ≤15초 (사실상 순간 포화)
    ↓
④ G1 Mixed GC 트리거 → Full GC 승격 → Major GC 1.65초 STW 🔴
    ↓
⑤ 1.65초 동안 모든 애플리케이션 쓰레드 정지:
    - Tomcat worker threads → HTTP 요청 수락 불가 → 큐에 쌓임
    - STOMP broker dispatch → 메시지 처리 정지
    - Scheduled Outbox relay → 쿼리 실행 불가
    - /actuator/prometheus scrape → 504 Timeout
    ↓
⑥ GC 끝난 직후 heap 여전히 높음 (해제된 건 일부 Young Gen만)
    + 1.65초 동안 쌓인 대기 요청·메시지가 한꺼번에 들어옴
    ↓
⑦ Tomcat thread pool이 대기 요청을 받으려 급팽창 72 → 180+
    → max-threads 200 접근 → 이후 요청 거부 시작
    ↓
⑧ CPU 70% 도달 — GC thread + 대기 큐 drain + broker dispatch 동시
    ↓
⑨ 또 Heap 꽉 참 → 또 Major GC → 반복 (Death Spiral)
    ↓
⑩ 서버 사실상 정지 상태
```

#### 2.7.3 왜 VU ~50에서 정확히 꺾이는가

- Simple Broker fan-out = **VU² × 2 dispatch/s** (각 VU가 초당 2회 SEND · 전원에게 broadcast)
  - VU 10 → 200/s (C-3.5 문제없음)
  - VU 50 → 5,000/s (broker thread + 객체 생성 rate 임계치)
  - VU 100 → 20,000/s
  - VU 1000 → 2,000,000/s (이론상 계산 불가능 수준)
- Heap 256 MiB로는 5,000 dispatch/s의 객체 churn을 Young GC로 수거 못 함 → Old Gen promotion 폭증 → Major GC 트리거
- Tomcat 200 threads도 50 WS 세션(각자 subscribe + send + receive 3 path) = 150 threads 점유에 + health check + actuator 등 감안하면 여유 없음
- CPU 2 vCPU (t3.medium 추정)로는 Simple Broker의 single-threaded dispatch + GC + 네트워크 I/O 병행 처리 한계

**결론**: 이 인프라 스펙 (256 MiB heap · 2 vCPU · Tomcat 기본)에서 **Simple Broker 기반 STOMP**는 VU 50 내외가 한계. 이는 Phase 3 설계 문서 ([learning/15](../learning/15-websocket-stomp-deep-dive.md), [learning/21](../learning/21-village-public-chat-architecture.md)) 의 "스케일아웃 필요 시 외부 broker 교체" 가정을 **실측으로 입증**한 결과.

#### 2.7.4 Prometheus scrape 공백이 확증하는 것

5개 패널 모두 17:22~17:35 구간 공백. 이건 단순히 "데이터 없음"이 아니라 **서버가 `/actuator/prometheus` 엔드포인트조차 응답 못 할 정도로 마비**되었다는 측정 가능한 증거. 블로그 Task 1에 "모니터링 시스템이 모니터링 대상을 못 따라잡았다" 서사로 사용 가능.

#### 2.7.5 수습 방향 — 3가지 선택지

| 옵션 | 방식 | 예상 효과 | 공수 | 리스크 |
|------|------|---------|-----|-------|
| **A. 단기 패치** — JVM heap `-Xmx 512m` + Tomcat `server.tomcat.threads.max=400` | deploy/.env · docker-compose 수정 · 재배포 | VU ~100~150 까지 상향 예상. Simple Broker 한계는 여전 | 30분 | 낮음. 근본 해결 아님 |
| **B. 근본 해결** — STOMP Simple Broker → Redis Pub/Sub (external) | WebSocketConfig에 `enableStompBrokerRelay` · Redis 인프라 · 설정 마이그레이션 | VU 500~1000+ 확장 가능. 멀티 인스턴스 지원 | 1~2일 | 중. 채팅 아키텍처 재설계 |
| **C. 코드 최적화** — position throttling (서버에서 10Hz → 2Hz 강제) · broker outbound thread pool 증설 | 컨트롤러·broker config 수정 | VU ~100 선까지 개선. 실시간 반응성 일부 저하 | 4시간 | 낮음. UX 트레이드오프 |

권장 조합: **A(즉시) → C(코드 최적화) → 부하 재측정 → 필요 시 B(장기)**.

---

### 2.5 재발 방지 설계

**즉시 복구**: `prepare-tokens.js COUNT=1000` 재실행 → login-only 경로로 ~60초 → 즉시 C-4 재돌입.

**단기** (다음 세션 반영):

- `prepare-tokens.js` 출력 포맷 변경: `{ generatedAt: ISO8601, tokens: [...] }` — 발급 시각 기록
- k6 실행 래퍼(shell)에서 `generatedAt` 읽어 55분 초과 시 warn · 60분 초과 시 abort

**중기**:

- 운영 `.env`에 `JWT_ACCESS_TOKEN_EXPIRY_MS=14400000` (4h)을 **부하 테스트 수행 직전만** 일시 적용 → 테스트 후 원복
- 또는 test-only endpoint `POST /api/v1/auth/test-token-bulk` 신설 (장기 JWT 발급, profile=prod 에서는 비활성)

**학습노트 반영 필요**:

- `docs/learning/41-k6-load-testing-setup.md` §7 "자주 만나는 함정"에 "7.7 JWT 만료 × 토큰 풀 재사용" 항목 추가

---

## 3. 관찰된 병목 징후 — 아직 미확정

VU 10까지는 이상 없음 (§2.2). C-4는 JWT 만료로 무효 — 재돌입 후에만 판정 가능.

**유력 1차 병목 후보** (재실행 시 관전 포인트): position broadcast fan-out (1000 VU × 2 Hz → 1000명 수신 = **초당 약 200만 메시지**).

---

## 4. 계획된 실행 — C-4 정식 부하

### 4.1 프로파일

```text
ramping-vus: 0 → 50 (1m) → 500 (3m) → 1000 (5m) → 0 (2m)
총 11분 (gracefulRampDown 30s 포함 시 약 11분 30초)
```

### 4.2 실행 커맨드

```bash
BASE_URL=https://ghworld.co WS_URL=wss://ghworld.co/ws/websocket \
  k6 run \
  --stage 1m:50 --stage 3m:500 --stage 5m:1000 --stage 2m:0 \
  --summary-export=loadtest/summary.json \
  loadtest/village-mixed.js
```

### 4.3 Grafana 준비 체크리스트

- [ ] Time range: **Last 15 minutes** + auto-refresh 5s
- [ ] 대시보드 2개 동시 열기: JVM 4701 + Spring Boot Statistics 10280
- [ ] 브라우저 별 창으로 `https://ghworld.co` 열어 Phaser 화면 캐릭터 이동 수동 관찰
- [ ] 스크린샷 시점: T+1m (VU 50) · T+4m (VU 500) · T+9m (VU 1000)

### 4.4 감시 핵심 패널 6종

| 패널 | 현재 idle 값 | 부하 중 경계선 |
|------|-------------|--------------|
| JVM Heap used | 57.6 / 256 MiB (22%) | **80%+ 지속 → 위험** |
| HTTP Rate | 0.156 ops/s | VU 증가와 비례해야 정상 |
| HTTP Duration avg / max | 4.17 / 16 ms | **avg 100ms+ / max 1s+ 이면 병목** |
| Threads live | 58 | **200+ 이면 pool saturation 임박** |
| GC CPU time | 0.0% | **2%+ 이면 GC 과부하** |
| CPU process | 0.2% | **80%+ 이면 CPU bound** |

### 4.5 선행 결정 (C-4 실행 전 확정 필요)

- [ ] NPC_ADAPTER=hardcoded 재확인 (운영 EC2 `.env`)
- [ ] 경로 최종 확정: Cloudflare vs Origin IP 직결
- [ ] 부하 시간대 (트래픽 적은 시간)
- [ ] (선택) Prometheus `--web.enable-remote-write-receiver` + k6 `--out experimental-prometheus-rw` 연동 여부

---

## 5. 파일 아카이브

- `loadtest/village-mixed.js` — 시나리오 스크립트
- `loadtest/lib/stomp.js` — STOMP 프레임 헬퍼
- `loadtest/prepare-tokens.js` — 토큰 풀 발급기 (login-first 멱등)
- `loadtest/tokens.json` — 1000 JWT (git ignored)
- `loadtest/summary.json` — k6 실행 요약 (C-4 후 생성, git ignored)
- `loadtest/screenshots/` — Grafana/Phaser 스크린샷 (C-4 후, git ignored 권장)

---

## 6. 업데이트 로그

| 시각 | 내용 |
|------|------|
| 2026-04-22 15:26 | C-3 스모크 (VU 1 / 30s) — 성공 |
| 2026-04-22 | C-3.5 중간 스모크 (VU 10 / 60s) — 성공, fan-out 10배 검증 |
| 2026-04-22 17:00 | C-4 정식 부하 — **실패** (JWT 만료 트랩, kill switch 발동 수동 중단) |
| 2026-04-22 17:07 | 근본 원인 확정: JWT 1h 만료 × 토큰 풀 1h 38m 경과. 부하 문제 아님 |
| 2026-04-22 17:21 | 토큰 재발급 + C-4 2차 실행 |
| 2026-04-22 17:22 | **VU 53에서 첫 502 에러 — 실제 breaking point 도달** |
| 2026-04-22 17:27 | T+6m07s (VU 713 도달) 수동 중단. 이후 백엔드 504 지속 |
| 2026-04-22 17:30 | 수동 재기동 후 복구 (200 OK) |
| 2026-04-22 17:39~17:42 | **데모 부하** (친구 시연 목적) — VU 20 · 3분 · position 1000ms · handshake 109/109 ✓ · stomp_errors 0 · p95 164ms. 안전 운영 검증. `loadtest/demo-summary.json` |
