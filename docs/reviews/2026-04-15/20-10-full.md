# 전체 프로젝트 리뷰 — 2026-04-15 20:10

## 대상
- 종류: full project scan (Codex MCP 호출)
- Codex threadId: 019d90cf-4f71-72d0-b79e-2ce6de0064de

## Codex 리뷰 결과

### [CRITICAL] 아키텍처 위반

- `RegisterUserService.java:31`, `UserPersistenceAdapter.java:30-31`, `UserLocalAuthJpaRepository.java:9`
  이메일 중복 검사를 `existsByEmail()`로 먼저 확인한 뒤 저장. check-then-act 패턴. 동시 가입 시 두 요청이 모두 pre-check를 통과할 수 있고, 한쪽은 DB unique 제약에서 터진 뒤 `DuplicateEmailException` 대신 500으로 떨어질 수 있음. `INSERT ... ON CONFLICT DO NOTHING` 또는 unique 위반을 도메인 예외로 변환 필요.

- `IdempotencyGuard.java:19-24`, `UserRegisteredEventConsumer.java:47-61`, `ConversationSummaryEventConsumer.java:57-86`
  Kafka 멱등성이 `existsByEventId()` 확인 후 `save()`하는 2단계로 구현. Rule 6 위반. 원자적 `insert-if-absent`가 아니라서 중복 소비 시 두 consumer가 동시에 통과 가능. 특히 `ConversationSummaryEventConsumer`는 LLM/embedding 호출이 `markAsProcessed()` 이전이라 중복 비용 발생 가능.

- `InitializeUserVillageService.java:30-36`
  마을 초기화가 `load(...).isPresent()` 후 `save`로 이어짐. Rule 6 위반. `character.user_id` unique에 일부 의존하지만, 코드 자체는 금지 패턴.

- `SendMessageService.java:108-116`
  참여자 생성도 `load()` 후 `save()` 후 재조회하는 check-then-act. V4 마이그레이션으로 unique 제약을 추가했어도 AGENTS.md 기준으로는 허용되지 않는 패턴.

- `UserRegisteredEventConsumer.java:54-57`
  `userId`가 없는 malformed 이벤트를 경고만 남기고 `markAsProcessed()` 처리해 영구 소거. "재처리 불가한 이벤트 유실". DLQ 또는 critical alert 후 미처리 상태 유지 필요.

- `ConversationSummaryEventConsumer.java:67-73`, `MessageCassandraPersistenceAdapter.java:31-34`
  요약 consumer가 사용자 메시지를 못 찾으면 `markAsProcessed()` 후 종료. 메시지 저장은 Cassandra 두 테이블에 비원자적으로 쓰이고, PostgreSQL outbox 트랜잭션과도 분리. 일시적인 쓰기 지연/불일치 시 요약 이벤트 영구 유실 가능.

- Cucumber Steps 전체 (`CommunicationSteps.java:23,26`, `HealthCheckSteps.java:25,28`, `IdentitySteps.java:25,28`, `VillageSteps.java:22,25`)
  `@Autowired` 필드 주입. Rule 3 위반.

- `NpcReplyServiceTest.java:80`
  테스트 코드에서 `new RuntimeException()` 직접 사용. Rule 4 위반.

### [WARNING] 컨벤션 위반

- `OutboxKafkaRelay.java:53-55`, `UserRegisteredEventConsumer.java:42-43`, `ConversationSummaryEventConsumer.java:52-53`
  `@Transactional`이 Service 계층이 아닌 scheduler/consumer에 존재. 서비스 계층 원칙과 불일치.

- `OutboxKafkaRelay.java:56-68`
  relay가 `PENDING` 조회 후 바로 발행하는데, claim/lock 없이 동작. 단일 인스턴스 전제이지만 인스턴스 증가 시 중복 발행 위험.

- Kafka consumer/outbox 전용 단위 테스트 부재. 장애 가능성 높은 코드인데 테스트 공백이 큼.

- `RegisterUserService.java:29-47` — 동시 요청 경쟁 조건 테스트 없음.

- `ChatMessageHandler.java:38-50`, `StompSendMessageRequest.java:3` — STOMP 입력 경로 검증 테스트 없음.

### [INFO] 참고 사항

- REST와 WebSocket의 메시지 본문 제약은 UseCase 커맨드에서 공통 검증. 입력 검증 규칙 자체의 불일치는 없음.
- Domain 패키지에 JPA/Spring 인프라 어노테이션 사례 없음 (LGTM).
- Domain 간 직접 참조 없음 (LGTM).
- `ConcurrentHashMap` 기반 메시지 카운터는 재시작/멀티 인스턴스 한계를 주석으로 명시.

### LGTM

- `HexagonalArchitectureTest.java:37-147` — ArchUnit으로 아키텍처 규칙 자동 검증.
- `SendMessageUseCase.java:22-27` — 메시지 본문 검증을 유즈케이스 커맨드에 모아둔 설계.
- `V4__participant_unique_constraint.sql:12-13` — participant 중복 생성 문제를 DB unique 제약으로 보강.
